Core Features to ImplementStaff-Specific SchedulingEach staff member has their own working hours per day.Each staff member can have different slot lengths (slotGranularity) like 15, 20, or 30 minutes.Staff can set breaks or split shifts within their working hours.Service-Based DurationsEach service has a duration in minutes.Service duration must be a multiple of the staff’s slotGranularity.When booking, the system must reserve multiple consecutive slots for longer services.Per-Day Slot GenerationEndpoint: POST /staff/:id/generate-slotsInput: { date: "YYYY-MM-DD" }Generates all available slots for the day based on working hours, breaks, and slot length.Save slots to a Slot collection:Core Features to ImplementStaff-Specific SchedulingEach staff member has their own working hours per day.Each staff member can have different slot lengths (slotGranularity) like 15, 20, or 30 minutes.Staff can set breaks or split shifts within their working hours.Service-Based DurationsEach service has a duration in minutes.Service duration must be a multiple of the staff’s slotGranularity.When booking, the system must reserve multiple consecutive slots for longer services.Per-Day Slot GenerationEndpoint: POST /staff/:id/generate-slotsInput: { date: "YYYY-MM-DD" }Generates all available slots for the day based on working hours, breaks, and slot length.Save slots to a Slot collection:Core Features to ImplementStaff-Specific SchedulingEach staff member has their own working hours per day.Each staff member can have different slot lengths (slotGranularity) like 15, 20, or 30 minutes.Staff can set breaks or split shifts within their working hours.Service-Based DurationsEach service has a duration in minutes.Service duration must be a multiple of the staff’s slotGranularity.When booking, the system must reserve multiple consecutive slots for longer services.Per-Day Slot GenerationEndpoint: POST /staff/:id/generate-slotsInput: { date: "YYYY-MM-DD" }Generates all available slots for the day based on working hours, breaks, and slot length.Save slots to a Slot collection:staffId, date, start, end, status ('available', 'booked', 'blocked'), bookingId (nullable)Booking SystemEndpoint: POST /bookingsInput: { staffId, serviceId, date, desiredStart, customerId }Steps:Calculate how many consecutive slots are needed for the service.Check if all required slots are available.Use a MongoDB transaction to:Mark those slots as booked with a bookingId.Create a Booking record.Prevent double booking.Cancel BookingEndpoint: DELETE /bookings/:idSets booking status to cancelled and frees up slots.Get Available SlotsEndpoint: GET /staff/:id/slots?date=YYYY-MM-DDReturns the staff’s slots with current availability.Real-Time UpdatesImplement WebSockets or SSE to push slot updates to clients whenever bookings are made or cancelled.Advanced Scheduling FeaturesCopy Day to Other Days: Duplicate a staff member’s schedule from one day to other selected days.Bulk Break Addition: Add the same break time (e.g., lunch) to all staff in one action.Special Days / Holidays: Mark a staff member or entire salon as closed for a specific day.Staff Self-Management: Staff can log in to set their own hours, breaks, and personal leave.RulesStore all times in UTC in the database.Prevent overlapping bookings for the same staff member.Breaks automatically remove time ranges from availability.Allow easy future extension to recurring schedules.DeliverablesMongoose models for Staff, Service, Slot, Booking.Express routes for slot generation, booking, cancellation, and availability.Real-time updates using WebSockets/SSE.Utility functions for generating slots from working hours + breaks.README explaining:How to run on Replit.env setup for MongoDBExample API calls (curl/Postman).Build the code so it can be dropped into my current salon booking app and work with minimal changes.